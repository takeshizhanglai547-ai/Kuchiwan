; =============================================================================
; DefaultEngine.ini — ASHLINE
;
; ここに書いた設定はエディタの「プロジェクト設定」と同じもの。
; エディタ側で変更すると、このファイルが書き換わる（それで正しい）。
; =============================================================================

[/Script/EngineSettings.GameMapsSettings]
; 起動時と再生時に開くレベル。
; ※ このレベルはまだ存在しない。RUNBOOK の手順3で
;    /Game/Maps/L_AshlineArena という名前で作ること。名前が1文字でも違うと
;    「再生を押すと真っ黒」になる。
EditorStartupMap=/Game/Maps/L_AshlineArena.L_AshlineArena
GameDefaultMap=/Game/Maps/L_AshlineArena.L_AshlineArena
; ルールの入れ物。ここを AshlineGameMode 以外にすると Sim が生成されず、
; Pawn が毎フレーム「AshlineGameMode が見つかりません」とログに出す。
GlobalDefaultGameMode=/Script/AshlineUE.AshlineGameMode

[/Script/Engine.LocalPlayer]
; 画角の基準にする軸を「横」に固定する。ここを固定しない理由が無い。
;
; コアの fov は three.js 由来の【垂直】画角で、UCameraComponent::FieldOfView は
; 【水平】画角なので、AshlineBridge::FovToUnreal が画面の縦横比を使って
; 垂直→水平に直している（16:9 で 垂直65° → 水平約97°）。
;
; ところが UE には「画面の形が変わったとき、縦と横のどちらの画角を維持するか」
; という設定が別にあり、これが MaintainYFOV になっていると、
; エンジンが FieldOfView を【縦】の画角として解釈し直して横へ広げ直す。
; つまり変換が2回かかり、16:9 で実際の水平画角が 127° 前後まで開く
; （魚眼のように歪み、遠くの敵が異様に小さくなる）。
;
; この設定はプロジェクトに書かないと、インストールされているエンジンの
; 既定値をそのまま引き継ぐ。どちらになるかは環境任せで、しかも
; 「なんとなく画角が変」としか見えないので原因に辿り着けない。だから固定する。
;
; MaintainXFOV = 横の画角を維持する ＝ FieldOfView を水平画角として扱う。
; AshlineBridge::FovToUnreal の前提はこちら。変えないこと。
AspectRatioAxisConstraint=AspectRatio_MaintainXFOV

[/Script/Engine.PhysicsSettings]
; ASHLINE の当たり判定は AshlineCore が持っていて、UEの物理は使っていない。
; それでも重力を既定のままにしてあるのは、演出用の破片やラグドールを
; 後から足したときに、UEの標準的な見え方から外れないようにするため。
DefaultGravityZ=-980.000000

[/Script/WindowsTargetPlatform.WindowsTargetSettings]
DefaultGraphicsRHI=DefaultGraphicsRHI_DX12
