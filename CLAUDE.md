# Kuchiwan — 作業メモ

## 報告の作法

- **作業完了の見込み時間を、指示されなくても必ず添える。** 着手前に「このバッチは約○分」「全体で約○時間」を示し、
  ずれた場合はその時点で更新する。見込みが立てられない場合は「立てられない理由」を言う。
- 検証していないことを検証したと書かない。テストが落ちていればその出力ごと報告する。
- 過去に何度も「サブエージェントの虚偽申告」と「自分のテストが自己証明になっていた」事故が起きている。
  数値を報告するときは、その数値をどう測ったかを1行添える。

## このリポジトリ

主対象は `beltaction.html`（単一ファイルHTML5 canvasベルトアクション「聖犬士イッヌ」、約11,300行）。
全ロジックが1つの `<script>` に入っている。

## 開発サイクル

実装 → 構文チェック → ヘッドレス15スイート → Playwrightで目視確認 → コミット → push →
（「マージして」と言われたら）PR作成 → squash-merge → ブランチを main と同期。

- ブランチは `claude/game-polish-graphics-audio-fh8hs3` のみ。他へは push しない。
- PRは「マージして」と言われたときだけ作る。

## テスト

```bash
bash tests/runall.sh          # 18スイート
```

テストは `tests/` にある（以前は `/tmp` に置いていたが、実行環境が使い捨てなので
セッションのたびに失われていた）。ドライバは `tests/*_driver.js`、ハーネスは
`tests/nm_head.js`。ランナーは各スイートを mktemp の別ファイルへ書き出してから実行する
（同じファイルを使い回すと cat の書き込み中に node が読んでモジュールローダーが落ちる）。

検証対象は `NM_TARGET` で差し替えられる。ミューテーションテストはこれを使う：

```bash
cp beltaction.html /tmp/mut.html   # /tmp/mut.html に改変を1つ入れる
NM_TARGET=/tmp/mut.html bash tests/runall.sh
```

**テストを足したら必ずミューテーションテストで赤くなることを確かめる。**
2026-08-09 の監査で、14件の改変が18スイート全緑を素通りした。
「免許皆伝」の告知コードを丸ごと削除しても誰も気付かなかった。

### 自己参照テストを書かない

`if(first > GAT_SPIN)` のように**検証対象の定数と比べる**と、その定数を0にした瞬間に
素通りする。`ATK_VAR[3].hits[0].z` から `±30` を作って天井を測るのも同じ。
期待値は直値で書くか、独立に測った値（跳躍の実測頂点など）を基準にする。

### 実装が使う関数をフックする

`drawBeastClaw` は `limbSeg` ではなく生の `ctx` で描く。`limbSeg` を張ると
**プレイヤーの腕**を測ってしまい、敵の振りは何も検証されない。
ヘッドレスの `ctx` はプロキシでメソッド差し替えが効かないので、`ctx` の束縛ごと
入れ替える（`const real=ctx; ctx=new Proxy(real,{get:...}); ... ctx=real;`）。

### テストを書くときの落とし穴

- **ドライバはテンプレートリテラルの中身**なので、`\*` のようなバックスラッシュは消える。
  正規表現は使わず `indexOf` で書く。
- **自己証明テストを書かないこと。** 期待値を自分で組み立てて比較すると、
  描画側だけに掛かる係数を構造的に見逃す。実際に描画関数へ渡される値を横取りして測る。
  （例：`foeLegTo` / `foeLegSolve` をラップして足先の目標座標を捕まえる）
- 更新と描画の順は本編と同じ「更新→描画」にする。逆にすると1フレームずれて偽の結果が出る。

## 性能

- 敵16体が同時に歩くシーンで約19ms。垂直同期の下限は16.7ms。
- 過去の事故：`ctx.filter` を描画ループで使い60fps→2.9fps／全画面合成／毎フレームのグラデ生成。
- 計測は必ず同一シーンで交互に、rAF間隔の中央値の min-of-5 で見る。ノイズ床は約0.6ms。
- **アトラクトデモは比較計測に使わない**（毎回キャラ・周回・敵が変わる）。
- 適応品質ガバナー `perfTier`（0/1/2）があるので、重い装飾はこれで段階的に落とす。
- **この実行環境の壁時計はあてにならないことがある。** 背景のA/Bで、同じ設定が
  実行ごとに 0.15ms と 5ms を行き来した。ブロック順を変えて測ると、速い遅いが
  「設定」ではなく「何ブロック目か」で入れ替わる＝環境側の周期的な要因。
  min-of-N でも消えない。疑わしいときは**同じ設定を連続ブロックで交互に測り**、
  設定と一緒に動くかを確かめる。動かないなら壁時計は捨てて、
  `ctx` の束縛を差し替えて**1フレームの描画コール数**を数える（決定論的で再現する）。

## 背景のレイアウト

テーマは `STAGE_THEME[]` に並ぶ。中景の地形は `land:'castle'` のように名前で指定し、
`LAND` テーブルの生成関数が呼ばれる（未指定なら従来の尾根 `drawRidgeRow`）。
手前のシルエットは `fg:'spire'` で指定する（未指定なら space/bug/stars から推定）。
**綴りを間違えると黙って既定の「四角い柱」に落ちる**ので、`tests/bg_driver.js` が
実装済みの種類との突き合わせと、種類ごとに形が違うことを検査している。

洞窟・海中のテーマ（`land:'cavern'/'sunken'`）では空の雲と鳥を止めている。
`stars:0` だけを条件にすると、水の中に雲が浮かぶ。

## Playwright

```bash
cd /tmp && NODE_PATH=/opt/node22/lib/node_modules node /tmp/shotX.js
```
`executablePath: '/opt/pw-browsers/chromium'`。ブラウザには `window.rafCb` が無いので、
ヘッドレス用の手動フレーム送りはそのまま使えない。止めたい場合は `hitStop=99999`。
ただし**立ち上がりのあるエフェクトは発生直後に凍らせると見えない**ので、可視域で止めること。

## 絵作りの方針

ドラゴンズクラウン級を目標にしている。厚塗り油彩調・陰影が深い・暖色のリムライト・
巨大なボスと小さな主人公の対比・装飾的な額縁UI・重量感のある溜めと振り抜き。

編曲・エフェクトともに層が密なので、**「持続音や持続する層を足す」変更は必ず衝突する**。
過去にブラス／聖歌隊／パッドの13thで3回とも失敗している。色を変えたいときは
リズム層や既存層の再配置で行う。

### 新しいキャラを起こすときは、まず明度を離す

肌・髪・衣・武器を近い明度で並べると、色相が違っても画面では1つの塊になる。
ゼウスの初稿は肌 `#e8d4a8`／髪 `#f6efd6`／衣 `#e6d9a8` で、顔も髭も衣も判別できず
「クリーム色の四角」になった。**先に明度を3段（暗い衣・中間の肌・明るい髪）に分けてから**
色相を乗せる。髭のように肌と接する面は、輪郭線を1本入れると確実に分かれる。

### リムライトのオフスクリーン枠から光や武器がはみ出すと四角く切れる

`rimBegin(hw,hh)` は指定した矩形しか転送しないので、光輪・長い槍・雷のように
体の外へ大きく出る要素は、枠の縁で**直線的に切り落とされる**。画面には薄い長方形の
継ぎ目として出る。枠を広げるか、要素を枠に収めること（神は `t.h+120` を渡している）。

### ヒットストップはパーティクルを止めない

`updateParticles` は `hitStop>0` の間もフレームの1/3で寿命を減らす。
`hitStop=99999` で凍らせても、900ms 待てば18フレーム分歳を取るので、
寿命12〜16の短命エフェクト（`bolt` など）は撮影までに消える。
撮影用に延ばすときは **`life` と `max` を同じ倍率で掛ける**こと。
比が見た目（大きさ・輝度）を決めるので、片方だけ変えると形が壊れる。

## 技の進化

コマンド技7枠は `SPECIAL_UP` で上位技IDに差し替わる（Lv3/5/7/9/10/10/12）。
タメ攻撃・掴み技・空中技・奥義は技IDを増やさず `evoTier`（Lv8=熟練／Lv16=極）で
威力と演出を伸ばす。投げと奥義のダメージは直値で書かれているので `TD()` で一括して掛ける。

## PI2 は2πではない

`const PI2=Math.PI/2` ——**四分の一回転**。`for(i<4) i*PI2` のように4分割で使うのが正しく、
`i/n*PI2` で全周のつもりに使うと90度の扇形になる。過去に4件の誤用があった
（トゲ・ガトリングの銃身・銃口炎・花火）。全周は `TAU` を使う。
